      var Death = 0;
      var Points =  0;
      function AddDeath(DeathData)
         {
             //Death Increments after each time function is called based on the behavior of the user
DeathData = this.Death++;

var ScoreCount = function Score(Pointdata)
{

   ScoreCount = this.Death++;
 ScoreCount = this.document.getElementById("Lives");
}
var DeathCount = function Death(DeathData)
 {
    DeathCount = this.Points++;
    DeathCount = this.document.getElementById("Loss");
}
        
}